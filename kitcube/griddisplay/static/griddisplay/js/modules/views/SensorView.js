define([
    'jquery', 
    'underscore', 
    'Backbone', 
    'text!templates/canvas/layout.html', 
    'common',
], function($, _, Backbone, Template) {

    var SensorView =  Backbone.View.extend({

    });

    return SensorView;
});
